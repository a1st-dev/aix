{
   "$schema": "https://turborepo.com/schema.json",
   "tasks": {
      "build": {
         "dependsOn": ["^build"],
         "outputs": ["dist/**", "schema.json", "oclif.manifest.json"]
      },
      "test": {
         "dependsOn": ["^build"]
      }
   }
}
