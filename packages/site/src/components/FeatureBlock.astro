---
const features = [
   {
      problem: 'Tired of duplicating config across editors?',
      solution: 'Define once in ai.json, install everywhere',
      icon: 'copy',
      snippet: 'aix install --target cursor',
   },
   {
      problem: 'Hard to share AI standards with your team?',
      solution: 'Extend configs from GitHub, npm, or local files',
      icon: 'share',
      snippet: 'aix add skill github:team/standards',
   },
   {
      problem: "Can't find the right MCP servers?",
      solution: 'Search and add from the official registry',
      icon: 'search',
      snippet: 'aix search mcp playwright',
   },
];
---

<section class="feature-block py-24 md:py-28">
   <div class="mx-auto max-w-5xl px-6">
      <h2 class="mb-4 text-balance text-center text-3xl font-bold text-gray-50 md:text-4xl">
         Stop fighting your tools
      </h2>
      <p class="mx-auto mb-16 max-w-2xl text-pretty text-center text-lg text-gray-300 md:text-xl">
         aix handles the configuration so you can focus on building.
      </p>

      <div class="grid gap-8 md:grid-cols-3">
         {features.map((feature) => (
            <div class="feature-block__card group rounded-2xl border border-gray-800/70 bg-gray-900/40 p-6 transition-all duration-300 hover:-translate-y-1 hover:border-gray-700 hover:bg-gray-900/70">
               <div class="mb-4 flex size-11 items-center justify-center rounded-xl bg-accent-500/10 text-accent-300 ring-1 ring-accent-500/20">
                  {feature.icon === 'copy' && (
                     <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                     </svg>
                  )}
                  {feature.icon === 'share' && (
                     <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" />
                     </svg>
                  )}
                  {feature.icon === 'search' && (
                     <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                     </svg>
                  )}
               </div>

               <p class="mb-2 text-sm font-medium text-gray-300">
                  {feature.problem}
               </p>
               <p class="mb-4 text-base font-semibold text-gray-50">
                  {feature.solution}
               </p>

               <div class="overflow-hidden rounded-lg border border-gray-800/70 bg-gray-950/80 p-3 font-mono text-sm text-gray-300">
                  <span class="text-accent-400">$</span>
                  <span class="ml-2">{feature.snippet}</span>
               </div>
            </div>
         ))}
      </div>
   </div>
</section>
