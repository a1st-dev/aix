<section class="support-legend" aria-labelledby="support-legend-title">
   <div class="support-legend__header">
      <p class="support-legend__eyebrow">How to read the statuses</p>
      <h2 id="support-legend-title">Support key</h2>
   </div>

   <div class="support-legend__grid">
      <section class="support-legend__group" aria-labelledby="support-legend-editor">
         <h3 id="support-legend-editor">Editor support</h3>
         <ul>
            <li>
               <span
                  aria-label="Supported: The editor exposes this feature at this scope."
                  class="support-availability support-availability--supported"
                  title="The editor exposes this feature at this scope."
               >
                  <span class="support-availability__label">Supported</span>
               </span>
               The editor exposes that feature at that scope.
            </li>
            <li>
               <span
                  aria-label="No support: The editor does not expose this feature at this scope."
                  class="support-availability support-availability--unsupported"
                  title="The editor does not expose this feature at this scope."
               >
                  <span class="support-availability__label">No support</span>
               </span>
               The editor does not expose that feature at that scope.
            </li>
         </ul>
      </section>

      <section class="support-legend__group" aria-labelledby="support-legend-aix">
         <h3 id="support-legend-aix">aix support</h3>
         <ul>
            <li>
               <span
                  aria-label="Native: aix writes the editor's own format."
                  class="support-status support-status--compact support-status--native"
                  title="aix writes the editor's own format."
               >
                  <span class="support-status__label">Native</span>
               </span>
               aix writes the editor&apos;s own format.
            </li>
            <li>
               <span
                  aria-label="Adapter: aix keeps the feature through an adapter or alternate representation."
                  class="support-status support-status--compact support-status--shim"
                  title="aix keeps the feature through an adapter or alternate representation."
               >
                  <span class="support-status__label">Adapter</span>
               </span>
               aix keeps the feature through an adapter or alternate representation.
            </li>
            <li>
               <span
                  aria-label="No support: aix does not write that feature for the destination today."
                  class="support-status support-status--compact support-status--unsupported"
                  title="aix does not write that feature for the destination today."
               >
                  <span class="support-status__label">No support</span>
               </span>
               aix does not write that feature for the destination today.
            </li>
         </ul>
      </section>
   </div>

   <p class="support-legend__footnote">
      Use the <abbr class="aix-term" title="Configuration written into the current repository or workspace.">Project scope</abbr>
      {' '}and <abbr class="aix-term" title="Configuration written into the editor&apos;s home or profile-level location.">User scope</abbr>
      {' '}rows to see where config lands at each level.
   </p>
</section>

<style>
   .support-legend {
      display: grid;
      gap: 1rem;
      margin-block: 1.75rem;
      padding: 1.1rem 1.15rem;
      border: 1px solid var(--sl-color-hairline-light);
      border-radius: 1rem;
      background: color-mix(in srgb, var(--sl-color-bg) 82%, var(--sl-color-accent-low));
   }

   .support-legend__header {
      display: grid;
      gap: 0.25rem;
   }

   .support-legend__header h2 {
      margin: 0;
      border: 0;
      padding: 0;
      font-size: 1.3rem;
   }

   .support-legend__eyebrow {
      margin: 0;
      color: var(--sl-color-gray-3);
      font-size: 0.74rem;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
   }

   .support-legend__grid {
      display: grid;
      gap: 1rem;
      grid-template-columns: repeat(auto-fit, minmax(18rem, 1fr));
   }

   .support-legend__group {
      display: grid;
      gap: 0.65rem;
      padding: 0.95rem 1rem;
      border: 1px solid var(--sl-color-hairline-light);
      border-radius: 0.85rem;
      background: color-mix(in srgb, var(--sl-color-bg) 88%, var(--sl-color-accent-low));
   }

   .support-legend__group h3 {
      margin: 0;
      font-size: 1rem;
   }

   .support-legend__group ul {
      display: grid;
      gap: 0.7rem;
      margin: 0;
      padding-inline-start: 1rem;
   }

   .support-legend__group li {
      display: grid;
      gap: 0.4rem;
   }

   .support-legend__footnote {
      margin: 0;
      color: var(--sl-color-gray-2);
   }

   .support-status,
   .support-availability {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      width: fit-content;
      padding: 0.2rem 0.55rem;
      border: 1px solid var(--sl-color-gray-5);
      border-radius: 999px;
      font-size: 0.78rem;
      line-height: 1;
      white-space: nowrap;
   }

   .support-status--compact {
      padding-inline: 0.45rem;
      font-size: 0.72rem;
   }

   .support-status--native,
   .support-availability--supported {
      background: color-mix(in srgb, var(--sl-color-green-high) 10%, transparent);
      border-color: color-mix(in srgb, var(--sl-color-green) 30%, var(--sl-color-gray-5));
   }

   .support-status--shim {
      background: color-mix(in srgb, var(--sl-color-orange-high) 10%, transparent);
      border-color: color-mix(in srgb, var(--sl-color-orange) 30%, var(--sl-color-gray-5));
   }

   .support-status--unsupported,
   .support-availability--unsupported {
      background: color-mix(in srgb, var(--sl-color-red-high) 8%, transparent);
      border-color: color-mix(in srgb, var(--sl-color-red) 30%, var(--sl-color-gray-5));
   }
</style>
