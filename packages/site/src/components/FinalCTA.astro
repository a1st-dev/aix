---
const installCommand = 'npm install -g @a1st/aix';
---

<section class="final-cta py-24 md:py-28">
   <div class="mx-auto max-w-3xl px-6 text-center">
      <h2 class="mb-4 text-balance text-3xl font-bold text-gray-50 md:text-4xl">
         Get started in seconds
      </h2>
      <p class="mx-auto mb-10 max-w-xl text-pretty text-lg text-gray-300 md:text-xl">
         Install aix globally and configure your first project.
      </p>

      <div class="final-cta__install mx-auto mb-8 flex max-w-md items-center justify-between gap-4 overflow-hidden rounded-xl border border-gray-800/70 bg-gray-900/60 p-4">
         <code class="font-mono text-sm text-gray-300 md:text-base">
            <span class="text-accent-400">$</span>
            <span class="ml-2">{installCommand}</span>
         </code>
         <button
            type="button"
            class="final-cta__copy shrink-0 rounded-lg border border-gray-800/70 bg-gray-900 p-2 text-gray-400 transition-colors hover:border-gray-700 hover:bg-gray-800 hover:text-gray-200"
            data-copy={installCommand}
            aria-label="Copy install command"
         >
            <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
               <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
         </button>
      </div>

      <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
         <a
            href="/getting-started/quick-start"
            class="inline-flex items-center justify-center rounded-lg bg-accent-500 px-6 py-3 text-base font-semibold text-gray-950 transition-all hover:bg-accent-400 hover:shadow-glow focus:outline-none focus:ring-2 focus:ring-accent-500 focus:ring-offset-2 focus:ring-offset-gray-950"
         >
            Read the docs
         </a>
         <a
            href="https://github.com/a1st-dev/aix"
            class="inline-flex items-center justify-center gap-2 rounded-lg border border-gray-700 bg-gray-800/50 px-6 py-3 text-base font-semibold text-gray-200 transition-all hover:border-gray-600 hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-accent-500 focus:ring-offset-2 focus:ring-offset-gray-950"
         >
            Star on GitHub
         </a>
      </div>
   </div>
</section>

<script>
   document.querySelectorAll('.final-cta__copy').forEach((button) => {
      button.addEventListener('click', async () => {
         const command = button.getAttribute('data-copy');
         if (command) {
            await navigator.clipboard.writeText(command);
            const svg = button.querySelector('svg');
            if (svg) {
               svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />';
               setTimeout(() => {
                  svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />';
               }, 2000);
            }
         }
      });
   });
</script>
