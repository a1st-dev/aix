---
research_performed_at: "2026-06-05T16:40:07-04:00"
editor_id: "opencode"
editor_version: "1.16.0"
sources:
   - "https://github.com/anomalyco/opencode/releases"
   - "https://opencode.ai/docs/config/"
   - "https://opencode.ai/docs/agents/"
---

## Changes affecting aix

- OpenCode 1.16.0 added skill discovery and file-based agent loading.
   - aix status: needs follow-up. aix already writes OpenCode skills under
     `.opencode/skills/{name}/` and agents under `.opencode/agents/*.md`, but this
     release should be checked against the current OpenCode docs to confirm whether new
     discovery paths or frontmatter fields are now preferred.
- OpenCode 1.16.0 added managed workspace cloning that keeps dirty and untracked files.
   - aix status: no generated config change needed.
- OpenCode 1.16.0 added moving sessions between workspaces and directories.
   - aix status: no generated config change needed.
- OpenCode 1.16.0 fixed Windows path normalization in migrated storage.
   - aix status: no direct config change needed, but keep Windows fixture coverage for
     OpenCode config discovery.
- OpenCode 1.16.0 fixed the GitHub extension so it refuses to commit without an existing
  git author identity.
   - aix status: no generated config change needed.

## Baseline evidence

- `packages/core/src/editors/strategies/opencode/import-utils.ts` parses OpenCode JSON and
  JSONC config.
- `packages/core/src/editors/strategies/opencode/prompts.ts` imports and writes OpenCode
  commands.
- `packages/core/src/editors/strategies/opencode/mcp.ts` writes OpenCode MCP config under
  top-level `mcp`.
- `packages/core/src/editors/adapters/opencode.ts` writes native OpenCode skills and
  agents.
- `packages/schema/src/editor-support.ts` documents OpenCode support and marks hooks
  unsupported.
