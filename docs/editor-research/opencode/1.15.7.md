---
research_performed_at: "2026-05-21T17:13:01-04:00"
editor_id: "opencode"
editor_version: "1.15.7"
sources:
   - "https://github.com/anomalyco/opencode/releases"
   - "https://opencode.ai/docs/config/"
   - "https://opencode.ai/docs/agents/"
---

## Changes affecting aix

- The current npm package version is 1.15.7. The GitHub releases page inspected during this run showed 1.15.6 as the latest detailed release note.
   - aix status: use 1.15.7 as the checked package version, but compare release-note details against 1.15.6 until the detailed 1.15.7 notes are visible.
- OpenCode 1.15.6 fixed agent and command name resolution from relative config paths.
   - aix status: already likely aligned. aix writes OpenCode `instructions` and `command` config using project-relative paths; keep this note for regression testing.
- OpenCode config supports JSON and JSONC, merged global/project/remote config, `instructions`, `command`, `mcp`, plugins, permissions, agents, and managed settings.
   - aix status: already partly aligned. aix supports `opencode.json` and `opencode.jsonc`, imports config-defined `instructions` and `command` prompts, writes MCP under top-level `mcp`, and treats hooks as unsupported.
- OpenCode agent docs mark `tools` as deprecated in favor of permissions.
   - aix status: needs follow-up if aix emits OpenCode agent files with `tools`. Current OpenCode support should prefer permission-oriented fields if agent output expands.
   - addressed in aix: implemented. `packages/core/src/editors/strategies/shared/agents.ts` now lets editor adapters rename or omit generic frontmatter fields, and `packages/core/src/editors/adapters/opencode.ts` writes `permission` instead of `permissions` while omitting deprecated `tools`.

## Baseline evidence

- `packages/core/src/editors/strategies/opencode/import-utils.ts` parses OpenCode JSON and JSONC config.
- `packages/core/src/editors/strategies/opencode/prompts.ts` imports and writes OpenCode commands/prompts.
- `packages/core/src/editors/strategies/opencode/mcp.ts` writes OpenCode MCP config under top-level `mcp`.
- `packages/schema/src/editor-support.ts` documents OpenCode support and marks hooks unsupported.
