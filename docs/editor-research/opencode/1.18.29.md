---
research_performed_at: "2026-09-07T20:15:00-04:00"
editor_id: "opencode"
editor_version: "1.18.29"
sources:
   - "https://github.com/anomalyco/opencode/releases"
   - "https://opencode.ai/docs/config/"
   - "https://www.npmjs.com/package/opencode-ai"
---

## Changes affecting aix

- OpenCode 1.18.29 updated Codex OAuth model filtering to recognize integer GPT versions
  such as `gpt-6`.
   - aix status: no change needed. Model resolution bugfix in OpenCode's provider adapter.
- OpenCode 1.18.28 sent the session identifier as GitHub Copilot's interaction header to
  improve cross-turn request correlation.
   - aix status: no change needed. Runtime networking enhancement.
- OpenCode 1.18.27 defaulted provider header timeouts and streamed chunk timeouts to five
  minutes (with `false` supported to disable), and added Anthropic `thinking.blockBinding`
  configuration support for Claude 5.1+ models.
   - aix status: no change needed. These are model provider execution settings and do not
     alter OpenCode workspace rules or MCP files.
- OpenCode 1.18.26 fixed tool call timing during live metadata updates and prevented
  `apply_patch` from emitting empty move paths in permission metadata.
   - aix status: no change needed. Runtime execution fix.
- OpenCode 1.18.24 enabled V1 runtimes to read supported V2 configuration fields so newer
  configuration files operate across mixed environments.
   - aix status: supported. aix generates standard OpenCode workspace configuration files
     compatible with current stable and forward runtimes.
- OpenCode 1.18.23 added native dashed slug conversion for Anthropic models routed through
  Cloudflare AI Gateway.
   - aix status: no change needed. Provider gateway routing fix.

## Baseline evidence

- `packages/core/src/editors/adapters/opencode.ts` manages OpenCode workspace instructions,
  skills, and tool configuration.
- `packages/schema/src/editor-support.ts` documents OpenCode capabilities and profiles.
