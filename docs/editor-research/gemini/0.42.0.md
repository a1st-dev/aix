---
research_performed_at: "2026-05-21T17:13:01-04:00"
editor_id: "gemini"
editor_version: "0.42.0"
sources:
   - "https://github.com/google-gemini/gemini-cli/blob/main/docs/changelogs/index.md"
---

## Changes affecting aix

- Gemini CLI 0.42.0 introduced an Auto Memory Inbox with a canonical-patch contract for skill management.
   - aix status: needs follow-up only if Gemini documents stable memory files or skill patch files that aix should manage. Current aix Gemini support writes rules, prompts, MCP, and hooks.
   - addressed in aix: no change needed in this pass. The checked docs describe command behavior rather than a stable memory file that aix should write. Existing Gemini strategy files remain focused on documented config files.
- Gemini CLI 0.40.0 introduced MCP resource tools and a four-tier memory management system.
   - aix status: no current MCP config change found in this run. If resource tool settings become user-configurable, update the Gemini MCP strategy.
- Gemini CLI 0.24.0 and later release notes describe Agent Skills support, `/skills reload`, and `/skills install/uninstall`.
   - aix status: needs follow-up. `packages/schema/src/editor-support.ts` should be checked to confirm whether Gemini skills are intentionally unsupported or now should be native.
   - addressed in aix: already supported. `packages/core/src/editors/adapters/gemini.ts` uses `NativeSkillsStrategy` for `.gemini/skills/{name}/` and `~/.gemini/skills/{name}/`, and `packages/schema/src/editor-support.ts` already marks Gemini skills as native.
- Gemini CLI hooks are configured in `.gemini/settings.json`.
   - aix status: already aligned. `packages/core/src/editors/strategies/gemini/hooks.ts` writes hooks into `.gemini/settings.json`.

## Baseline evidence

- `packages/core/src/editors/strategies/gemini/hooks.ts` maps aix hook events to Gemini CLI hook names and writes them into `.gemini/settings.json`.
- `packages/core/src/editors/strategies/gemini/mcp.ts` owns Gemini MCP config output.
- `packages/schema/src/editor-support.ts` documents Gemini's current aix feature support.
