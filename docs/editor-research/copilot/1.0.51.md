---
research_performed_at: "2026-05-21T17:13:01-04:00"
editor_id: "copilot"
editor_version: "1.0.51"
sources:
   - "https://raw.githubusercontent.com/github/copilot-cli/main/changelog.md"
   - "https://docs.github.com/en/copilot/reference/custom-agents-configuration"
---

## Changes affecting aix

- The current npm package version is 1.0.51. The public Copilot CLI changelog inspected during this run listed detailed notes through 1.0.49.
   - aix status: use 1.0.51 as the checked package version, but compare future work against 1.0.49 release-note details unless GitHub publishes missing 1.0.50 and 1.0.51 notes.
- Copilot CLI 1.0.49 says repo hooks in `.github/hooks/` load in prompt mode when the folder is already trusted.
   - aix status: already aligned. `packages/core/src/editors/strategies/copilot/hooks.ts` writes project hooks under `.github/hooks/hooks.json`.
- Copilot CLI 1.0.49 accepts MCP server configs with no `args` field and treats it as an empty args list.
   - aix status: no code change needed unless aix validation rejects missing args for Copilot MCP output. Existing MCP normalization should be checked before changing code.
   - addressed in aix: no change needed. `packages/core/src/editors/strategies/copilot/mcp.ts` only emits `args` when the normalized stdio transport has arguments, and its import path accepts command-only servers.
- Copilot CLI 1.0.49 reports MCP stdio servers as `stdio` instead of `local`.
   - aix status: needs follow-up only if generated Copilot MCP config still emits a deprecated local type. Inspect `packages/core/src/editors/strategies/copilot/mcp.ts` before changing.
   - addressed in aix: no change made. GitHub's custom agent MCP docs still show `type: 'local'` for local command servers, while the CLI changelog describes display terminology. `packages/core/src/editors/strategies/copilot/mcp.ts` keeps the documented config value.
- GitHub custom agents support `tools`, `mcp-servers`, `metadata`, `disable-model-invocation`, and `user-invocable`; `argument-hint` and `handoffs` are ignored by the cloud agent.
   - aix status: needs follow-up if aix expands Copilot agent support beyond the currently documented fields.
   - addressed in aix: no change needed in this pass. `packages/core/src/editors/adapters/copilot.ts` already maps agent MCP config to `mcp-servers`, and unsupported cloud-only fields are not generated by default.

## Baseline evidence

- `packages/core/src/editors/strategies/copilot/hooks.ts` maps aix hooks to Copilot CLI hook events and writes project hooks to `.github/hooks/hooks.json`.
- `packages/core/src/editors/strategies/copilot/mcp.ts` owns Copilot MCP output.
- `packages/core/src/editors/strategies/copilot/prompts.ts` owns Copilot prompt conversion behavior.
- `packages/schema/src/editor-support.ts` documents Copilot MCP, hooks, prompts, skills, and agents support.
