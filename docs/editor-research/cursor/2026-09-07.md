---
research_performed_at: "2026-09-07T20:15:00-04:00"
editor_id: "cursor"
editor_version: "3.11"
sources:
   - "https://cursor.com/changelog"
   - "https://docs.cursor.com/hooks"
---

## Changes affecting aix

No aix change is needed for this range.

Cursor continues its unversioned changelog format since 3.11 (2026-07-10). Per the filing
convention documented in `.aix/skills/editor-release-research/SKILL.md`, this document is
dated `2026-09-07.md` and preserves `editor_version: "3.11"`.

The changes published on Cursor's changelog since the previous research pass on 2026-08-24
are:

- Start from scratch (2026-08-27): Introduced enhanced initial prompt options and empty-state
  workflow suggestions in the Composer agent interface.
   - aix status: no change needed. This is an internal UI enhancement for Composer and
     does not alter configuration formats.
- Self-hosted machines (2026-09-02): Enabled running Cursor Cloud Agents on private
  infrastructure and self-hosted worker pools.
   - aix status: no change needed. Infrastructure configuration for cloud agents is
     managed via the Cursor web dashboard and does not affect local workspace files.
- Cursor hook definitions remain standardized at `.cursor/hooks.json` with `version: 1`
  envelope formatting.
   - aix status: supported. `packages/core/src/editors/strategies/cursor/hooks.ts` emits
     compliant `version: 1` hook configuration files.

## Baseline evidence

- `packages/core/src/editors/strategies/cursor/hooks.ts` formats Cursor hook configs with
  `version: 1`.
- `packages/core/src/editors/strategies/cursor/rules.ts` manages `.cursor/rules/*.mdc`.
- `packages/core/src/editors/adapters/cursor.ts` wires rules, commands, MCP, skills, agents,
  and hooks.
