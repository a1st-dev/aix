---
research_performed_at: "2026-06-05T16:40:07-04:00"
editor_id: "cursor"
editor_version: "3.7"
sources:
   - "https://cursor.com/changelog"
   - "https://docs.cursor.com/"
---

## Changes affecting aix

- Cursor 3.7 added Design Mode improvements in the Cursor browser, including
  multi-select elements and voice input while an agent is running.
   - aix status: no generated config change needed. This is editor UI behavior, not a
     rules, commands, MCP, skills, agents, or hooks format change.
- Cursor 3.7 added Cursor SDK support for custom metadata stores, custom local tools,
  local-tool auto-review, and nested subagents.
   - aix status: no current implementation change. These SDK surfaces are separate from
     the local editor config files aix writes.
- Cursor 3.7 added a context usage report and canvas Design Mode changes.
   - aix status: no generated config change needed.
- Cursor 3.7 SDK releases changed run metadata and local shell packaging behavior.
   - aix status: no current implementation change. Re-check only if aix starts generating
     Cursor SDK scripts or SDK config.

## Baseline evidence

- `packages/core/src/editors/strategies/cursor/hooks.ts` writes Cursor hooks with a
  `version: 1` wrapper.
- `packages/core/src/editors/strategies/cursor/prompts.ts` writes Cursor commands.
- `packages/core/src/editors/strategies/shared/standard-mcp.ts` owns Cursor MCP output.
- `packages/core/src/editors/adapters/cursor.ts` wires Cursor native rules, commands,
  MCP, skills, agents, and hooks.
- `packages/schema/src/editor-support.ts` documents Cursor native support for the aix
  feature set.
