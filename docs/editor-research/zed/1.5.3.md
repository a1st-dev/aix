---
research_performed_at: "2026-06-05T16:40:07-04:00"
editor_id: "zed"
editor_version: "1.5.3"
sources:
   - "https://zed.dev/releases/stable"
   - "https://zed.dev/docs"
---

## Changes affecting aix

- Zed 1.5.3 fixed commit message generation so it respects instructions from global
  `AGENTS.md`.
   - aix status: no generated config change needed. This confirms that the Zed 1.4.2
     global rule path `~/.config/zed/AGENTS.md` remains relevant.
- Zed 1.5.3 fixed deprecated Rules being automatically included in new agent requests.
   - aix status: no generated config change needed. aix already moved Zed skills to
     native `.agents/skills/{name}/` support in the prior research pass.
- Zed 1.5.3 fixed global skill edits appearing without a file path in change summaries.
   - aix status: no generated config change needed. This affects runtime display after
     aix writes global skills.
- Zed 1.5.3 fixed UTF-8 parsing issues when loading skill frontmatter with multi-codepoint
  graphemes.
   - aix status: no generated config change needed. aix writes skill frontmatter as plain
     YAML; keep tests focused on ASCII unless the repo intentionally supports non-ASCII
     skill metadata.
- Zed 1.4.4 and 1.4.3 only show Copilot-chat model dropdown fixes in the stable release
  page.
   - aix status: no generated config change needed.

## Baseline evidence

- `packages/core/src/editors/strategies/zed/rules.ts` writes project `.rules` and
  user-scope `~/.config/zed/AGENTS.md`.
- `packages/core/src/editors/adapters/zed.ts` uses native Zed Agent Skills at
  `.agents/skills/{name}/`.
- `packages/core/src/editors/strategies/zed/mcp.ts` owns Zed MCP config.
- `packages/schema/src/editor-support.ts` documents Zed's current aix feature support.
